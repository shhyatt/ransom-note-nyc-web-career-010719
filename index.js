function binaryMatch(letters){
  let sortedLetters = letters.sort();
  let frequency = {};
  for (let i = 0; i < sortedLetter.length; i++){
    let char = sortedLetter.charAt(i)
    if(frequency[char]){
      frequency[char]++
    }else{
      frequency[char] = 1
    }
  }
  
}